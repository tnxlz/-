var name='甄嬛';
var  age=18;
function sum(num1,num2){
  return num1+num2
}

function priceNum(price,format){
  var number=format ||2;
  return price.toFixed(number)
}
module.exports={
  name:name,
  age:age,
  sum:sum,
  priceNum:priceNum
}